# DEPLOY-1 — F3: Cutover da Evolution (DTI -> VPS) — 2026-07-02

Status: **CUTOVER CONCLUÍDO. A produção do msgautomation é o VPS Hostinger.**
Regra de ouro respeitada: em nenhum momento houve duas Evolution conectadas ao número.

## Linha do tempo (UTC do servidor; SP = UTC-3)

| Hora | Passo | Onde |
|---|---|---|
| ~20:5x | Fabio congela o DTI: kill switch OFF + services parados | DTI |
| 21:05 | Dump final incremental gerado e enviado (16,7 MB) | DTI -> VPS |
| 21:06 | Services locais parados; dump final restaurado: 3.076 -> 3.347 incoming (+271 do intervalo) | VPS |
| 21:07 | Reaplicados os ajustes locais que o dump sobrescreve: credentials do canal limpas via model -> `msg:channel:sync-env` (apikey NOVA cifrada; verificado), token do webhook conferido banco==Evolution, migrate no-op, services religados | VPS |
| 21:08 | Fabio desconecta (logout) a instância `fabio-pessoal` na Evolution do DTI | DTI |
| 21:08 | QR gerado (`evolution:qr`) e escaneado pelo Fabio | VPS |
| 21:09 | `state=open` — número conectado NA PRODUÇÃO NOVA | VPS |
| 21:09 | Robô religado pelo caminho oficial: **robô ON, IA OFF, proativas OFF** | VPS |
| 21:10 | Validação 1: "Que horas são?" (DTI - ENGEPEÇAS) — journal completo (Process 57ms -> Kanban -> SendAutoReply 877ms), resposta `sent` "Agora são 18:10 !" (hora SP correta), card em "Em atendimento" atualizado | VPS |
| 21:12 | Validação 2: fluxo "Menu" fim a fim — sessão 10 de "Atendimento (exemplo)" `completed` em 11s, menu + opção respondidos (logs 48 e 49) | VPS |

## Estado final dos canais e freios

- Canal 1 `fabio-pessoal` (evolution): conectado no VPS, credenciais cifradas com a
  apikey nova local, webhook por token (MT-2) — validado com tráfego real.
- Switches: robô ON, IA OFF, proativas OFF (proativas vieram ON no dump inicial e
  foram desligadas pelo caminho oficial no F2; o congelamento do DTI trouxe tudo OFF
  no dump final e o robô foi religado só após a conexão).
- Freios/tetos: os do dump (por conta, Redis 6380 novo — contadores zerados no VPS).
- Única campanha proativa: `done` (nada armado).

## DTI rebaixado a DEV

Checklist pro Fabio aplicar lá (registrado também no docs/09): kill switch OFF
permanente, instância desconectada permanente (NÃO reescanear QR lá), services
`disable`, banco vira massa de dev, dump final guardado 30 dias como rollback.

**Rollback enquanto o dump viver:** logout no VPS -> QR no DTI -> religar services lá.

## Pendências herdadas (não bloqueiam)

- SSH do VPS segue com senha habilitada (decisão do Fabio; fail2ban ativo).
- `LOG_LEVEL=debug` mantido do DTI.

## Próximo: F4 (GATE) — CH-2 Parte B

Cloud API oficial com número de teste, agora sem firewall corporativo:
`graph.facebook.com` responde do VPS (~3,5 ms). Aguardando o ok do Fabio.
