# DEPLOY-1 — F1: Verificação + fundação (VPS Hostinger) — 2026-07-02

Status: **F1 CONCLUÍDO** (+ preparação não-secreta do F2 adiantada). Pendências que
dependem do Fabio listadas no fim.

## Viabilidade (F1.1) — APROVADO

| Item | Encontrado | Veredito |
|---|---|---|
| Acesso | root | ok |
| OS | Ubuntu 24.04.4 LTS (Noble), kernel 6.8 | ok (DTI era 22.04 — sem impacto) |
| systemd | PID 1, estado `running` | ok |
| Docker | 29.6.1 já instalado | ok |
| RAM | 7.8 GiB + 2 GiB swap | folga boa (bem acima dos 4 GiB mínimos) |
| Disco | 96 GiB, 85 GiB livres | ok |
| CPU | 2 vCPU x86_64 | ok |
| Região | Campinas-SP (AS Hostinger, srv1780620.hstgr.cloud, IP 187.127.24.165) | ótima latência BR |
| Latência Meta | graph.facebook.com ping ~3.5 ms, 0% loss | pré-requisito do F4 promissor |

## DESCOBERTA IMPORTANTE: o VPS NÃO estava vazio

Este servidor já é produção do **Nextgest** (outro projeto do Fabio):
- nginx (80/443) servindo o Nextgest; MySQL 8.0.46 com `nextgest_central` + `tenant_teste`;
  redis-server do sistema em 127.0.0.1:6379; php8.5-fpm.
- **Evolution API própria do Nextgest** em Docker há 30h: containers `evolution-api`
  (127.0.0.1:8088), `evolution-postgres`, `evolution-redis` — compose em `/srv/www/evolution/`.
- **NADA do Nextgest foi tocado.** O isolamento previsto no compose do repo
  (projeto `msgautomation_evo`, portas 8090/6380, redes próprias) funcionou como desenhado.

## Hardening (F1.2)

- ufw: já estava ativo com deny incoming; permite 22, 80, 443 (80/443 são do Nextgest).
  Adicionada 1 regra: `allow from 172.19.0.0/16 to any port 8080/tcp` (subnet interna
  `evonet_msg` — o container da Evolution precisa entregar webhook no app do host).
  8080/3306/6380/8090 seguem inacessíveis da internet.
- fail2ban: instalado e ativo, jail `sshd` rodando.
- SSH: chave presente em `/root/.ssh/authorized_keys`, MAS `PasswordAuthentication yes`
  ainda vence (50-cloud-init.conf). **NÃO desativei senha** — regra do prompt: só com
  confirmação do Fabio de que o acesso por chave funciona. PENDENTE (ver fim).

## Stack (F1.3) — já estava nas versões do pacote

| Componente | Pacote (DTI) | VPS | |
|---|---|---|---|
| PHP | 8.5.7 | 8.5.7 (todas as extensões Laravel presentes) | igual |
| MySQL | 8.0.46 | 8.0.46 | igual |
| Node | 22.22.3 | 22.23.0 | ok (patch acima) |
| Composer | 2.9.8 | 2.10.1 | ok |
| Evolution | v2.3.7 | v2.3.7 (imagem evoapicloud) | igual |
| Redis app | redis:7-alpine container | `msgautomation_redis` em 127.0.0.1:6380 | igual |

MySQL root: autenticação via socket unix local (padrão do servidor, já em uso pelo
Nextgest) — não foi trocada senha de root (servidor compartilhado). A aplicação usa
usuário próprio `msgautomation@localhost` (criado pelo script de segredos).

## Clone + build (F1.4)

- Repo em `/srv/www/msgautomation` (HEAD `94c923f`, DEPLOY-0).
- `composer install` ok; `npm ci` (0 vulnerabilidades) + `npm run build` ok (Vite, 740ms).

## Preparação do F2 já feita (nada que dependa de segredo ficou pendente)

1. **Dump restaurado**: banco `msgautomation` criado, 48 tabelas, contagens sanas
   (1 account, 1 channel `fabio-pessoal` provider evolution com webhook_token,
   3.076 incoming_messages, 1 user).
2. **Stack Evolution do msgautomation no ar, SEM instância/QR**: `evolution_msg`
   (127.0.0.1:8090, healthy, respondendo v2.3.7), `evolution_msg_postgres`,
   `evolution_msg_redis`, `msgautomation_redis` (6380, PONG). apikey NOVA gerada
   (64 hex) direto em `docker/evolution/.env` (chmod 600) — nunca exibida.
3. **`.env` do app** montado do inventário (63 chaves) com não-segredos preenchidos
   e `__PREENCHER__` nos segredos; chmod 600. Ajustes de VPS aplicados:
   `EVOLUTION_BASE_URL=http://127.0.0.1:8090`, `EVOLUTION_WEBHOOK_URL=
   http://host.docker.internal:8080/webhook/evolution` (rota por token: o
   `evolution:setup` anexa `/{webhook_token}`), `REDIS_PORT=6380` (predis),
   `QUEUE_CONNECTION=redis`, `CACHE_STORE=redis`, `TENANCY_SINGLE_ACCOUNT_FALLBACK=false`,
   `APP_ENV=production`, `APP_DEBUG=false`.
4. **Units systemd** copiadas de `deploy/systemd/` (paths já corretos), habilitadas e
   **paradas de propósito** — só iniciam depois do `.env` completo.
5. **Script de segredos**: `/root/msgautomation-migracao/preencher-env.sh` (prompt
   oculto com confirmação dupla; injeta a apikey nova da Evolution sozinho; cria o
   usuário MySQL da aplicação; `config:clear`).

## Avisos e pendências pro Fabio

1. **Estado do dump difere do esperado**: robô ON, IA OFF, **PROATIVAS ON**
   (`proactive_enabled=1`) — o prompt esperava proativas OFF. Única campanha existente
   está `done` (nada armado pra disparar), então não há risco imediato; confirmar no
   gate do F3 se desliga ou mantém.
2. **SSH**: confirmar que o login por chave funciona pra eu desativar
   `PasswordAuthentication` (fail2ban já mitiga enquanto isso).
3. Rodar `preencher-env.sh` (segredos por prompt oculto) pra liberar a validação do F2.
4. Decidir no túnel Cloudflare: só `/webhook/*` (default) ou painel com Cloudflare
   Access também. Valores de `AUTH_NAME`/`AUTH_EMAIL` e locale ficaram nos defaults
   do example — revisar se o DTI usava outros.
