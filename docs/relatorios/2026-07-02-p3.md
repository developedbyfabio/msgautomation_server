# Relatorio P-3 — Disparo real das proativas (tick + claim atomico + Sender proactive) — TUDO OFF

Data: 2026-07-02 (sequencia noturna 2/3). Baseline antes: **416 verdes**. Suite final:
**432 verdes** (15 ProactiveDispatchTest + 1 gate), 1376 asserts, sequencial. Os 416
anteriores passaram SEM mudanca de expectativa.

## Confirmacoes de seguranca
- **NADA ligado:** `proactive_enabled` OFF em producao; zero campanhas aprovadas reais;
  zero opt-ins reais. O tick roda e nao faz nada (so contas com switch ON entram).
- HTTP 100% mockado nos testes; `Http::assertNothingSent()` fora dos caminhos mockados.
- Robo reativo intocado; worker reiniciado limpo; /up 200.

## O que entrou

**Migration `000031` (aditivo):** `campaign_targets.sent_auto_reply_log_id` (auditoria
envio<->log) e `auto_reply_logs.campaign_id` (origem proactive no log; mode='proactive').

**Tick (`proactive:tick`, schedule a cada minuto):** por conta COM `proactive_enabled`,
busca targets pending vencidos (scheduled_at <= agora) de campanhas approved|running e
ENFILEIRA `SendProactiveMessage` (o tick NAO envia) — lote pequeno por rodada
(config proactive.tick_batch, default 5; nunca raja).

**Job `SendProactiveMessage`** — a sequencia blindada:
1. **claim do TARGET** (UPDATE pending->processing atomico): corrida de tick duplo/worker
   duplo perde e sai — ZERO duplicata (provado por teste);
2. **guard.allows** (os 9 freios da P-1, todos);
3. **claim ATOMICO dos tetos** (diario conta + semanal contato, com rollback);
4. render local dos placeholders -> **Sender modo `proactive`** com **R2 proprio no
   instante do POST** (volatileRecheck: switch, opt-out/opt-in, janela, segredo);
   R2 bloqueou = o job **devolve o claim** (`release`) — nada e gasto.

**Resultados tratados por motivo:**
- sent: target sent + sent_at + log origem proactive/campaign_id; campanha vira `done`
  quando nada resta.
- `teto_dia_proativo`: reagenda pra AMANHA na janela (a campanha continua; ninguem se
  perde). `teto_semana_contato`: +7 dias. `fora_da_janela_proativa`: proxima abertura.
  `fluxo_ativo`: +1h. `proactive_off`: pending SEM novo horario (aguarda religar).
- `opt_out`/`sem_opt_in`/`grupo`/`contem_senha`: **skipped definitivo** com motivo.
- Erro transitorio de HTTP: devolve claim + target volta a pending + LANCA pro retry
  padrao da fila (tries=3, backoff 30/60s); esgotou -> `failed()` marca failed/erro_envio.
  Retry re-claima — jamais duplica.

**Estados completados:** approved -> running (1o processado) -> done | paused | cancelled.
Pausar segura o tick; retomar reagenda os vencidos (janela+jitter). Des-aprovar BLOQUEADO
com target sent. **Opt-out no meio** (palavra OU manual) pula o contato em TODAS as
campanhas (skipped/opt_out_revogado) — testado nos dois caminhos.

**UI:** /campanhas com barra de progresso real (sent/skipped/failed/pending), modal de
destinatarios (status+motivo+hora), Pausar/Retomar/Cancelar com modais, badge
"aguardando interruptor" quando ha aprovada e o switch esta OFF. Card Proativas de
/configuracoes mostra consumo do dia x teto. Cada decisao do job logada (info/warning).

## ATENCAO — scheduler (1 passo de infra pro Fabio)
O schedule esta REGISTRADO (`php artisan schedule:list` mostra o tick por minuto), mas
**nao ha processo de scheduler rodando** em producao (so serve+worker). Criei o unit de
REFERENCIA `deploy/systemd/msgautomation-scheduler.service` — instalar e parte do
checklist de manha (nao instalei de madrugada: config viva e gate seu).

## CHECKLIST DE MANHA (primeiro disparo real controlado)
1. Instalar o scheduler: `sudo cp deploy/systemd/msgautomation-scheduler.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now msgautomation-scheduler` e conferir `systemctl status msgautomation-scheduler`.
2. Em /contatos: dar **opt-in em 1 contato de teste SEU** (o toggle registra o consentimento).
3. Em /campanhas: criar campanha "teste" com publico = esse contato, mensagem inocua (ex.: "Oi! Teste do sistema, pode ignorar.").
4. Preview (conferir a lista EXATA) -> **Aprovar** (modal forte).
5. Em /configuracoes: **ligar o interruptor de proativas** (modal explica o risco).
6. Observar: envio no minuto da janela/jitter; barra de progresso em /campanhas; log em auto_reply_logs (mode=proactive); consumo do dia no card Proativas.
7. Do contato de teste, mandar **PARAR** e conferir a revogacao (opt-in cai + consent revoke/palavra + targets pendentes pulados).
8. **DESLIGAR o interruptor** ao fim do teste.

## Proximo
M-1 (painel /painel) — iniciando em seguida, conforme as regras da sequencia noturna.
