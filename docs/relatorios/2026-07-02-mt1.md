# MT-1 — Multi-usuario (users <-> accounts) — 2026-07-02

**Status: ENTREGUE — aguardando GATE do Fabio (login + navegacao) antes do MT-2.**

## Pre-requisitos verificados
- Baseline 485 verdes (pos CH-1). MissingAccountContextException em PRODUCAO: zero
  (2 matches no laravel.log sao do env `testing`, madrugada do MT-0 — os proprios
  testes que provam a excecao; journal de producao: 0).

## O que entrou
- `account_user` (account_id, user_id, role owner|operador — D3: operador so no
  schema, sem UI). Backfill idempotente: usuario existente virou OWNER da conta 1
  (verificado em producao: user 1 -> conta 1, role owner).
- `SetAccountContext` (o unico ponto, como previsto no MT-0): a conta do request
  web vem do VINCULO do usuario logado, re-resolvida A CADA request (sessao
  forjada com conta alheia e resetada — autorizacao por request, nao so por
  query). Logado sem vinculo = 403 claro (logout continua acessivel).
- Fallback fase-1 DESLIGADO: config/tenancy default `false` (producao roda
  estrito; suite legada mantem semantica fase-1 via phpunit.xml, e os testes
  MT-1 desligam explicito). Query sem contexto fora do web = excecao alta.
- Seletor de conta ativa na navbar (sessao) — invisivel com 1 conta; rota
  POST /conta-ativa valida o vinculo (403 fora dele).
- Comando `msg:user:create {email} {--account=}` — senha via prompt OCULTO
  (nunca em argumento/log), minimo 10, confirmacao; vinculo owner idempotente.
- Auth ja tinha (conferido, nada a endurecer alem): throttle de login (5/min),
  session()->regenerate() no login, logout com invalidate+regenerateToken em
  todas as telas (header). 2FA/HTTPS seguem horizonte D6.

## Testes
485 -> 494 verdes (8 MultiUserTest + 1 extensao do gate TenantIsolationTest:
usuarios espelhados A/B via HTTP sem fallback, incluindo sessao forjada).
Nenhuma expectativa anterior mudou.

## Smoke de producao (conta 1)
- GET / -> 302 login; GET /login -> 200; /conversas sem auth -> 302.
- Webhook vivo: POST real 200 + worker processou (journal) sem persistir
  evento nao-mensagem. Config da Evolution INTOCADA.
- proactive:tick manual sem fallback: OK (itera contas explicito).
- config('tenancy.single_account_fallback') em producao: false.

## GATE MT-1 (Fabio)
1. Fazer login normal em http://192.168.11.210:8080 com seu usuario.
2. Navegar: conversas, contatos, regras, kanban, campanhas, configuracoes.
3. Confirmar que esta tudo identico (o seletor de conta NAO aparece — 1 conta).
Aprovando no chat, sigo pro MT-2 (canal por conta + migracao do webhook, que
tem o proprio sub-gate na troca da URL).
