# MT-2 — Canal/instancia por conta + migracao do webhook vivo — 2026-07-02

**Status: ENTREGUE E VALIDADO COM MENSAGEM REAL. MT-3 NAO EXECUTA — aguarda a
conta 2 ser real (ordem do Fabio).**

## Commits
- `ed2cd0c` parte 1: provisionamento + credenciais no banco + telas/comandos por canal
- `8267661` fix headers objeto (bug real da Evolution v2.3.7) + migracao aplicada (gate)
- `bdff7e6` passo final: secret global removido (separado e reversivel)

## O que entrou
- `ChannelProvisioner`: canal novo por conta (instancia `conta-{id}-{slug}` unica,
  token proprio, credenciais cifradas com default do env) + instancia na Evolution
  (`POST /instance/create`, endpoint em uso desde a Camada 1) + webhook por TOKEN.
  Idempotente. NUNCA troca webhook de instancia viva divergente.
- Credenciais por canal: `msg:channel:sync-env` migrou o env pro canal 1 (cifrado;
  idempotente; env agora = so default de provisionamento). Fallback do CH-1 segue.
- Canal DA CONTA do contexto em: gate de conexao, Conexao/QR, StatusConexao (sync
  de status SO no proprio canal), GroupNameResolver, `evolution:qr|status|setup
  --account`.
- `evolution:webhook:migrate`: dry-run por default; `--apply` so com gate;
  `--rollback` (pos-remocao do secret, exige git revert do `bdff7e6` junto).
- /configuracoes: card do canal (instancia, estado, URL do webhook mascarada,
  origem das credenciais).

## A migracao do webhook vivo (o passo com gate)
1. Dry-run apresentado ao Fabio; aprovado no chat.
2. 1a tentativa: HTTP 400 — a Evolution v2.3.7 exige `webhook.headers` como
   OBJETO json (array vazio do PHP = lista). Nada mudou na falha. Fix: cast
   `(object)` + teste fixando o contrato (`"headers":{}`).
3. `--apply` ok; `findWebhook` confirmou a URL por token, enabled.
4. VALIDACAO REAL: mensagens organicas (Claudia) entrando 100% pela rota
   `/webhook/evolution/{token}` no journal do serve; fluxo "Menu" respondendo
   fim a fim pela URL nova.
   (Nota: "Que horas sao?" dela nao respondeu POR ESCOPO da regra 3 —
   contatos especificos DTI/Alex — comportamento correto, nao relacionado.)
5. Secret global REMOVIDO (`bdff7e6`): URL antiga = 401 SEMPRE (smoke provou
   401 com o secret outrora correto; token = 200 com worker processando).

## Ajustes de expectativa (a mudanca E a migracao)
- TenantIsolationTest: secret global valido 200 -> 401 (teste renomeado).
- WebhookEvolutionTest/AutoReplyWireTest/SyncResilienciaTest: posts pela rota
  por token (setup); +2 testes novos (401 da URL antiga; token invalido).

## Numeros
494 (pos-MT-1) -> 502 verdes: +6 ChannelPerAccountTest, +1 gate de isolamento
(canal/credenciais da B invisiveis e intocaveis no contexto A), +2 webhook
(-1 consolidado). Regra 3 tambem ajustada por DADO via RuleWriter (contains
"que horas" tolerante) — pedido do Fabio, sem codigo.
