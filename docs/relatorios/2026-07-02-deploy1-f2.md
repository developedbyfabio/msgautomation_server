# DEPLOY-1 — F2: Dados e app dormindo (VPS Hostinger) — 2026-07-02

Status: **F2 CONCLUÍDO (túnel incluso).** Robô segue vivo SÓ no DTI — a Evolution
daqui está de pé e DESCONECTADA (sem QR). Aguardando gate do Fabio pro F3.

## Auditoria e correção do .env (copiado manualmente do DTI pelo Fabio)

- `chmod 600`, dono `root:root`. Valores jamais exibidos (checagens booleanas).
- Corrigido pro VPS: `APP_URL` (apontava pro IP da LAN do DTI → `http://127.0.0.1:8080`),
  `EVOLUTION_API_KEY` (veio a antiga do DTI → substituída pela apikey NOVA gerada aqui,
  a mesma do `docker/evolution/.env`), `TENANCY_SINGLE_ACCOUNT_FALLBACK=false`
  acrescentado explícito (estava ausente; o default do config já era false).
- Conferido e já correto: APP_KEY/SECRETS_KEY/GEMINI_API_KEY presentes, DB_* locais,
  `REDIS_PORT=6380` (predis, prefixo próprio), `EVOLUTION_BASE_URL=127.0.0.1:8090`,
  `EVOLUTION_WEBHOOK_URL=http://host.docker.internal:8080/webhook/evolution`,
  fila/cache no Redis.
- `APP_ENV=production` e `APP_DEBUG=false` aplicados (vieram local/true do DTI;
  corrigidos a pedido do Fabio na validação do túnel). `LOG_LEVEL=debug` mantido.
- MySQL: usuário `msgautomation@localhost` criado com a senha do .env, grant só no
  banco `msgautomation`.

## Banco e canal

- Dump restaurado (F1): 48 tabelas. `php artisan migrate`: **Nothing to migrate** (no-op).
- **Prova real das chaves sobre dados cifrados NO DTI:**
  - APP_KEY: decifrou `channels.credentials` do canal 1 vindas do dump
    (chaves base_url/apikey/instance; apikey de 64 chars ≠ da nova local → era a antiga
    do DTI). Valores não exibidos.
  - SECRETS_KEY: `SecretVault::reveal` abriu o secret `Wifi_Claudia` (8 chars, não exibido).
- **Credenciais do canal atualizadas pelo caminho oficial**: credentials limpas via model
  → `msg:channel:sync-env --account=1` regravou cifrado com a apikey NOVA (verificado).
- **Proativas desligadas pelo caminho oficial** (model AutoReplySetting, contexto conta 1):
  vieram ON no dump → OFF. Estado final: **robô ON, IA OFF, proativas OFF**
  (única campanha do dump está `done`, nada armado).

## Evolution local (dormindo)

- Stack `msgautomation_evo` no ar: `evolution_msg` (127.0.0.1:8090, healthy),
  postgres e redis próprios, `msgautomation_redis` (6380). Isolada da Evolution do
  Nextgest (8088).
- `evolution:setup --account=1`: instância `fabio-pessoal` criada, estado **close**
  (desconectada, sem QR). Webhook da instância: rota por token (MT-2), URL confere com
  `channels.webhook_token` do banco, evento `MESSAGES_UPSERT`, habilitado.
- Ponta container→host validada: `evolution_msg` alcança `host.docker.internal:8080`
  (regra ufw da subnet Docker funcionando).

## Suíte e app

- `php artisan test`: **530/530 verdes, 1.855 assertions, 15s** — o mesmo verde do DTI.
  (Primeira rodada falhou por falta de `php8.5-sqlite3` no VPS — extensão instalada;
  a suíte roda em SQLite `:memory:`, nunca no banco de produção.)
- systemd ativos: `msgautomation-serve` (8080), `-worker` (fila default), `-scheduler`
  (tick rodando; inofensivo com proativas OFF).
- App responde: `GET /` → 302 `/login`; `/login` → 200 "Entrar — msgautomation".
- **Nenhum canal conectado. Nada responde ninguém.**

## Túnel Cloudflare — VALIDADO

- `cloudflared 2026.6.1` em systemd (token por prompt oculto do Fabio), túnel ativo.
- Hostnames: `wa.nextgest.com.br` (path `webhook/*`) e `painel.nextgest.com.br`
  (Cloudflare Access com One-time PIN no e-mail do Fabio).
- Validações sem enviar payload real:
  - `POST wa.nextgest.com.br/webhook/evolution/{token-inválido}` → **401** (chega no
    app e é recusado sem credencial — rota por token viva de fora);
  - `GET wa.nextgest.com.br/` → **404 do túnel** (raiz não alcança o app);
  - `GET painel.nextgest.com.br/login` → **302 pro Cloudflare Access** (PIN antes do login);
  - re-teste pós APP_ENV=production: login local 200, webhook externo 401 — tudo de pé.

## Próximo: F3 (GATE — cutover, ~10 min com o Fabio presente)

QR aqui → desconecta DTI → validação real ("Que horas são?", fluxo Menu, Kanban)
→ registro em docs/09. Regra de ouro vigiada o tempo todo.
